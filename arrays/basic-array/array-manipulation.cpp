#include <bits/stdc++.h>

using namespace std;

typedef vector<int> vi;

int main()
{
    vi x = {1, 2, 3, 4, 5};
    for (int i = 0; i <= x.size() - 1; ++i)
    {
        cout << x.at(i) << "\n";
    }
}